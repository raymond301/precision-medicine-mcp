# LLM Provider API Keys
# Anthropic API Key (required for Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini API Key (optional - for Gemini provider on Cloud Run)
# Only used when provider selection is enabled (Cloud Run deployment)
# GEMINI_API_KEY=your_gemini_api_key_here

# Environment mode (required for local development)
# Set to "development" to disable SSO authentication requirement
# Set to "production" when deploying with Azure AD SSO
ENVIRONMENT=development

# Mock MCP servers for local testing (true/false)
# When true: Uses mock tool responses, no Cloud Run connections needed
# When false: Connects to real MCP servers on Cloud Run
# Recommended: true for local development, false for deployed environments
USE_MOCK_MCP=true

# Optional: Set default model
# DEFAULT_MODEL=claude-sonnet-4-5

# Optional: Set default max tokens
# DEFAULT_MAX_TOKENS=4096
