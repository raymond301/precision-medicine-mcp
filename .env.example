# ============================================================
# Precision Medicine MCP Platform — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env with real keys — it is already in .gitignore.
#
# Per-component .env.example files with full options:
#   ui/streamlit-app/.env.example
#   ui/streamlit-app-students/.env.example
#   ui/jupyter-notebook/.env.example
#   servers/mcp-multiomics/.env.example
#   infrastructure/deployment/.env.gcp
# ============================================================

# --- LLM Provider API Keys (at least one required) ----------
ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# --- GCP Project (required for Cloud Run deployment) --------
GCP_PROJECT_ID=
GCP_REGION=us-central1

# --- GCP Storage --------------------------------------------
PATIENT_DATA_BUCKET=
RESULTS_BUCKET=

# --- GCP Healthcare API (FHIR) ------------------------------
HEALTHCARE_DATASET=
FHIR_STORE=
HEALTHCARE_LOCATION=us-central1

# --- Epic FHIR (stored in GCP Secret Manager) ---------------
# These reference Secret Manager paths, not raw credentials.
# EPIC_CLIENT_ID_SECRET=projects/<PROJECT>/secrets/epic-client-id
# EPIC_CLIENT_SECRET_SECRET=projects/<PROJECT>/secrets/epic-client-secret
# EPIC_FHIR_ENDPOINT_SECRET=projects/<PROJECT>/secrets/epic-fhir-endpoint

# --- Azure AD SSO (stored in GCP Secret Manager) ------------
# AZURE_AD_CLIENT_ID_SECRET=projects/<PROJECT>/secrets/azure-ad-client-id
# AZURE_AD_CLIENT_SECRET_SECRET=projects/<PROJECT>/secrets/azure-ad-client-secret
# AZURE_AD_TENANT_ID_SECRET=projects/<PROJECT>/secrets/azure-ad-tenant-id

# --- MCP Server Defaults ------------------------------------
# All servers default to DRY_RUN=true (synthetic data, safe for testing).
# Set to false for production with real bioinformatics tools.
# FGBIO_DRY_RUN=true
# MULTIOMICS_DRY_RUN=true
# SPATIAL_DRY_RUN=true

# --- Streamlit App ------------------------------------------
ENVIRONMENT=development
USE_MOCK_MCP=true
# DEFAULT_MODEL=claude-sonnet-4-6
# DEFAULT_MAX_TOKENS=4096
