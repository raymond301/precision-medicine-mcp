% Ensure graphics can be included
\usepackage{graphicx}

% Use sans-serif font throughout
\renewcommand{\familydefault}{\sfdefault}

% Better URL handling and line breaking
% Note: Quarto loads hyperref automatically, so we don't load it again
\PassOptionsToPackage{hyphens}{url}
\usepackage{xurl}

% Configure hyperref options (Quarto loads this already)
\hypersetup{
    breaklinks=true,
    colorlinks=true,
    unicode=true,
    pdfborder={0 0 0}
}

% Allow URLs to break at these characters
\def\UrlBreaks{\do\/\do-\do_\do.\do=\do?\do\&\do\#}
\urlstyle{same}
