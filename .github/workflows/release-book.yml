name: Release Book PDF

on:
  push:
    tags:
      - 'v*.*.*'  # Trigger on version tags like v1.0.0, v1.1.0, etc.

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to create releases

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js for Mermaid
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Mermaid CLI
        run: |
          npm install -g @mermaid-js/mermaid-cli
          mmdc --version

      - name: Concatenate book chapters in order
        run: |
          cd docs/book

          # Extract version from tag
          VERSION="${GITHUB_REF#refs/tags/}"
          echo "Building book version: $VERSION"

          # Create title page with version
          cat > book-full.md << 'EOF'
---
title: "Building AI-Orchestrated Precision Oncology Systems"
subtitle: "A Practical Guide to MCP-Based Bioinformatics"
author: "Lynn Langit"
date: "2026-02-01"
toc: true
toc-depth: 2
numbersections: true
header-includes:
  - \setcounter{chapter}{-1}
include-before:
  - \setcounter{chapter}{-1}
linkcolor: blue
urlcolor: blue
geometry: margin=1in
fontsize: 11pt
documentclass: report
---

EOF

          # Concatenate all chapters in order
          cat chapter-00-introduction.md >> book-full.md

          # Part 1: Why This Matters
          for chapter in chapter-01-the-patientone-story.md \
                         chapter-02-the-architecture-problem.md \
                         chapter-03-testing-the-hypothesis.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Part 2: Building the Foundation
          for chapter in chapter-04-clinical-data.md \
                         chapter-05-genomic-foundations.md \
                         chapter-06-multiomics-integration.md \
                         chapter-07-spatial-transcriptomics.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Part 3: Advanced Capabilities
          for chapter in chapter-08-cell-segmentation.md \
                         chapter-09-treatment-response-prediction.md \
                         chapter-10-quantum-celltype-fidelity.md \
                         chapter-11-imaging-histopathology.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Part 4: Deployment and Operations
          for chapter in chapter-12-cloud-deployment-gcp.md \
                         chapter-13-hospital-production-deployment.md \
                         chapter-14-operations-monitoring.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Part 5: Research and Education
          for chapter in chapter-15-for-researchers.md \
                         chapter-16-teaching-precision-medicine.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Part 6: The Future
          for chapter in chapter-17-funding-sustainability.md \
                         chapter-18-lessons-learned.md; do
            if [ -f "$chapter" ]; then
              cat "$chapter" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          # Appendices - insert \appendix command to switch from numbered chapters to lettered appendices
          echo -e "\n\n\\appendix\n\n" >> book-full.md

          for appendix in appendix-a-quick-reference.md \
                          appendix-b-installation-setup.md \
                          appendix-c-patientone-dataset.md \
                          appendix-d-bias-and-ethics.md; do
            if [ -f "$appendix" ]; then
              cat "$appendix" >> book-full.md
              echo -e "\n\n\\newpage\n\n" >> book-full.md
            fi
          done

          echo "✅ Created book-full.md ($(wc -l < book-full.md) lines)"

      - name: Convert Mermaid diagrams to images
        run: |
          cd docs/book
          mkdir -p mermaid-images

          echo "🔄 Converting Mermaid diagrams..."

          # Python script to extract and convert mermaid blocks
          python3 << 'PYTHON_SCRIPT'
          import re
          import subprocess
          import os

          print("📖 Reading book-full.md...")
          with open('book-full.md', 'r') as f:
              content = f.read()

          print(f"   File size: {len(content)} characters")

          # Find all mermaid code blocks
          pattern = r'```mermaid\n(.*?)```'
          matches = list(re.finditer(pattern, content, re.DOTALL))

          print(f"\n🔍 Found {len(matches)} Mermaid diagrams")

          if len(matches) == 0:
              print("⚠️  WARNING: No mermaid diagrams found in book-full.md")
              print("   Check if chapters contain ```mermaid blocks")

          converted_count = 0
          for i, match in enumerate(matches):
              mermaid_code = match.group(1)
              mmd_file = f'mermaid-images/diagram-{i}.mmd'
              png_file = f'mermaid-images/diagram-{i}.png'

              print(f"\n📝 Processing diagram {i}:")
              print(f"   Code length: {len(mermaid_code)} chars")
              print(f"   First line: {mermaid_code.split(chr(10))[0][:50]}...")

              # Write mermaid code to file
              with open(mmd_file, 'w') as f:
                  f.write(mermaid_code)
              print(f"   ✅ Wrote {mmd_file}")

              # Convert to PNG using mmdc
              try:
                  result = subprocess.run([
                      'mmdc',
                      '-i', mmd_file,
                      '-o', png_file,
                      '-b', 'white'
                  ], check=True, capture_output=True, text=True)

                  # Verify PNG was created
                  if os.path.exists(png_file):
                      size = os.path.getsize(png_file)
                      print(f"   ✅ Created {png_file} ({size} bytes)")
                      converted_count += 1

                      # Replace mermaid block with image reference
                      old_text = match.group(0)
                      new_text = f'![Diagram {i}](mermaid-images/diagram-{i}.png)'
                      content = content.replace(old_text, new_text)
                      print(f"   ✅ Replaced code block with image reference")
                  else:
                      print(f"   ❌ PNG file not created!")

              except subprocess.CalledProcessError as e:
                  print(f"   ❌ mmdc failed: {e}")
                  print(f"   stdout: {e.stdout}")
                  print(f"   stderr: {e.stderr}")

          print(f"\n📊 Conversion Summary:")
          print(f"   Total diagrams found: {len(matches)}")
          print(f"   Successfully converted: {converted_count}")
          print(f"   Failed: {len(matches) - converted_count}")

          # Write modified content
          with open('book-with-images.md', 'w') as f:
              f.write(content)

          print(f"\n✅ Created book-with-images.md")

          # Verify no mermaid code blocks remain
          remaining = content.count('```mermaid')
          if remaining > 0:
              print(f"\n❌ ERROR: {remaining} mermaid blocks still present after conversion!")
              print("   This means the replacement failed - checking why...")
              # Show first remaining block for debugging
              first_block = content[content.find('```mermaid'):content.find('```mermaid')+100]
              print(f"   First remaining block: {first_block}...")
              exit(1)
          else:
              print(f"✅ All mermaid blocks successfully replaced with images")
          PYTHON_SCRIPT

      - name: Validate Mermaid conversion
        run: |
          cd docs/book

          echo "📊 Validation Results:"
          echo "Generated PNG files:"
          ls -lh mermaid-images/*.png 2>/dev/null || echo "⚠️  No PNG files found!"

          echo ""
          echo "Checking for remaining mermaid code blocks..."
          if grep -q '```mermaid' book-with-images.md; then
            echo "❌ FAIL: Mermaid code blocks still present in book-with-images.md"
            grep -n '```mermaid' book-with-images.md | head -5
            exit 1
          else
            echo "✅ PASS: No mermaid code blocks found"
          fi

          echo ""
          echo "Checking for image references..."
          IMAGE_COUNT=$(grep -c 'mermaid-images/diagram-' book-with-images.md || echo "0")
          echo "Found $IMAGE_COUNT image references to mermaid-images/"

          if [ "$IMAGE_COUNT" -eq 0 ]; then
            echo "❌ FAIL: No mermaid image references found"
            exit 1
          fi


      - name: Convert Markdown to PDF
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          cd docs/book

          echo "📝 Converting book-with-images.md to PDF..."
          echo "Version: $VERSION"
          echo "Book source lines: $(wc -l < book-with-images.md)"

          # Mount the entire workspace so relative image paths work correctly
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace/docs/book \
            pandoc/latex:latest \
            book-with-images.md \
            -o "precision-medicine-mcp-book-${VERSION}.pdf" \
            --resource-path=.:/workspace/data/images:/workspace/docs/book/mermaid-images \
            --pdf-engine=xelatex \
            --include-in-header=header.tex \
            --top-level-division=chapter \
            --toc \
            --toc-depth=2 \
            --number-sections \
            --highlight-style=tango \
            --variable linkcolor=blue \
            --variable urlcolor=blue \
            --variable geometry:margin=1in \
            --variable fontsize=11pt \
            --verbose

          # Verify PDF was created
          if [ ! -f "precision-medicine-mcp-book-${VERSION}.pdf" ]; then
            echo "❌ ERROR: PDF file was not created!"
            exit 1
          fi

          echo "✅ PDF created successfully"
          ls -lh "precision-medicine-mcp-book-${VERSION}.pdf"

          # Verify file is not empty
          PDF_SIZE=$(stat -f%z "precision-medicine-mcp-book-${VERSION}.pdf" 2>/dev/null || stat -c%s "precision-medicine-mcp-book-${VERSION}.pdf")
          if [ "$PDF_SIZE" -lt 1000 ]; then
            echo "❌ ERROR: PDF file is too small ($PDF_SIZE bytes)"
            exit 1
          fi
          echo "PDF size: $PDF_SIZE bytes"

      - name: Generate release notes
        id: release_notes
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          cd docs/book

          cat > release-notes.md << EOF
          ## 📖 Building AI-Orchestrated Precision Oncology Systems

          **Version**: ${VERSION}
          **Date**: $(date +'%Y-%m-%d')

          ### Book Contents
          - **18 Chapters**: Complete guide from concept to production deployment
          - **4 Appendices**: Quick reference, setup guide, dataset reference, ethics framework
          - **18 Jupyter Notebooks**: Hands-on companion exercises
          - **14 Mermaid Diagrams**: Architecture and workflow visualizations
          - **10 Screenshots**: UI examples and dashboards

          ### What's Included
          - ✅ All 18 chapters (Introduction through Lessons Learned)
          - ✅ Complete appendices (Quick Reference, Setup, PatientOne Dataset, Bias & Ethics)
          - ✅ Production-ready MCP server implementations
          - ✅ PatientOne synthetic dataset (100% safe to share)

          ### Book Statistics
          - **Total Pages**: ~$(pdfinfo precision-medicine-mcp-book-${VERSION}.pdf 2>/dev/null | grep Pages | awk '{print $2}' || echo "TBD")
          - **Total Words**: $(wc -w < book-full.md)
          - **PDF Size**: $(du -h precision-medicine-mcp-book-${VERSION}.pdf | cut -f1)

          ### Quick Links
          - **GitHub Repository**: https://github.com/lynnlangit/precision-medicine-mcp
          - **Companion Notebooks**: https://github.com/lynnlangit/precision-medicine-mcp/tree/main/docs/book/companion-notebooks
          - **PatientOne Dataset**: https://github.com/lynnlangit/precision-medicine-mcp/tree/main/data/patient-data/PAT001-OVC-2025

          ### Requirements
          To use the companion materials, you'll need:
          - Python 3.11+ (3.10 for DeepCell)
          - Google Cloud Platform account (for MCP server deployment)
          - Anthropic API key or Google AI API key

          ### Citation
          \`\`\`
          Langit, L. (2026). Building AI-Orchestrated Precision Oncology Systems:
          A Practical Guide to MCP-Based Bioinformatics (${VERSION}).
          GitHub. https://github.com/lynnlangit/precision-medicine-mcp
          \`\`\`

          ---

          **License**: Book content © 2026 Lynn Langit. Code examples: Apache 2.0.
          EOF

          # Set output for use in release creation
          echo "notes_file=docs/book/release-notes.md" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            docs/book/precision-medicine-mcp-book-${{ github.ref_name }}.pdf
          body_path: docs/book/release-notes.md
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload PDF as artifact (backup)
        uses: actions/upload-artifact@v4
        with:
          name: book-pdf-${{ github.ref_name }}
          path: docs/book/precision-medicine-mcp-book-${{ github.ref_name }}.pdf
          retention-days: 365
